from flask import Flask, request
from jwt import JET, jwt_required

from security import authenticate, identity

app = Flask(__name__)
app.secret_key = "Secret"

@app.route("/items",["POST,GET"])
def items:
    
